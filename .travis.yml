language: python
python:
    - "3.6"
install:
    - pip install -r requirements.txt
env:
  global:
    - export CONFIG_NAME=deployment
services:
  - postgresql

defore_script:
  - "psql -c 'CREATE DATABASE store-manager-main;' -U postgres"

after_success:
  - coveralls
script:
 - py.test --cov